{
  "name": "page-dashboard",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port 4000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint ./src"
  },
  "dependencies": {
    "@repo/mypack": "workspace:*",
    "@repo/tailwind-config": "workspace:*"
  },
  "devDependencies": {
    "nuxt": "^3.14.159",
    "vue": "latest",
    "vue-router": "latest",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*"
  }
}
