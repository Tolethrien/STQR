import type { Config } from "tailwindcss";
const baseConfig: Config = {
  extend: {},
  plugins: [],
  content: [], // add your own src in all apps to prevent broad scope
};
export default baseConfig;
